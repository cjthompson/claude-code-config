---
description: List all plans with progress and drift
---

Follow the project-tasks skill's instructions for the "list plans" trigger.
If `$ARGUMENTS` names a status, filter by it the same way `task-list` filters
tasks by status.
