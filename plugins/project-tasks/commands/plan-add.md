---
description: Create a new plan (inline or from a file)
---

Treat `$ARGUMENTS` exactly as the text after the project-tasks skill's
`plan:` prefix — a plan description for an inline plan, or an absolute file
path to import/link.

Follow the project-tasks skill's Plans instructions for that trigger,
including the Import or Link prompt when `$ARGUMENTS` is a path.
