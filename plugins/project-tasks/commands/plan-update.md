---
description: Re-sync a plan against its source and reconcile tasks
---

Parse `$ARGUMENTS` as a plan seq (`PNNN` or bare `N`).

Follow the project-tasks skill's instructions for "update plan PNNN" —
re-read the source, propose a diff, and reconcile tasks.
