---
description: Dispatch a plan's unblocked pending tasks
---

Parse `$ARGUMENTS` as a plan seq (`PNNN` or bare `N`).

Follow the project-tasks skill's instructions for "run plan PNNN" — dispatch
that plan's unblocked pending tasks, the same way "run all tasks" does but
scoped to this plan.
